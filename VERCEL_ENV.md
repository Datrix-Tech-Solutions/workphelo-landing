# Vercel Environment Variables

Set these variables in Vercel for the `Production`, `Preview`, and `Development` environments as needed:

- `SMTP_HOST`
- `SMTP_PORT`
- `SMTP_SECURE`
- `SMTP_USER`
- `SMTP_PASS`
- `SMTP_FROM`
- `WAITLIST_TO_EMAIL`
